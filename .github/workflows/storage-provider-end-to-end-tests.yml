name: Storage Provider UI End to End Tests

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  storage-provider-ui-end-to-end-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src

    services:
      postgres:
        image: postgres
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: gw_storage_provider
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - uses: actions/cache@v5
        env:
          cache-name: cache-pnpm-modules
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ matrix.node-version }}-${{ hashFiles('**/package.json') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-${{ matrix.node-version }}-

      - uses: pnpm/action-setup@v6.0.3
        with:
          version: 9.15.3

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "pnpm"
          cache-dependency-path: src/pnpm-lock.yaml

      # minio/minio image CMD is only `minio` (prints help). It needs `server /data`, which
      # jobs.services cannot pass after the image (see actions/runner#2139). Start it here instead.
      # Map host 9010 -> container 9000 so presigned uploads do not collide with Graphweaver's
      # admin UI, which binds port 9000 on the runner.
      - name: Start MinIO
        run: |
          docker run -d --name gw-minio \
            -p 9010:9000 \
            -e MINIO_ROOT_USER=minioadmin \
            -e MINIO_ROOT_PASSWORD=minioadmin \
            minio/minio:latest \
            server /data

      - name: Configure MinIO (S3)
        run: |
          for _ in $(seq 1 60); do
            if curl -sf http://127.0.0.1:9010/minio/health/live; then
              break
            fi
            sleep 1
          done
          curl -sf http://127.0.0.1:9010/minio/health/live
          aws configure set aws_access_key_id minioadmin
          aws configure set aws_secret_access_key minioadmin
          aws configure set default.region us-east-1
          aws s3 mb s3://my-bucket --endpoint-url=http://127.0.0.1:9010

      - name: Install dependencies
        run: pnpm i

      - name: Build CLI tool
        run: pnpm build

      - name: Install Playwright's dependencies
        working-directory: src/packages/end-to-end
        run: pnpm playwright install --with-deps chromium

      - name: Make envfile
        uses: SpicyPizza/create-envfile@v2.0
        with:
          envkey_AWS_S3_BUCKET: my-bucket
          envkey_AWS_S3_ENDPOINT: http://127.0.0.1:9010
          envkey_AWS_REGION: "us-east-1"
          envkey_AWS_ACCESS_KEY_ID: minioadmin
          envkey_AWS_SECRET_ACCESS_KEY: minioadmin
          envkey_PGHOST: localhost
          envkey_PGPORT: 5432
          envkey_PGDATABASE: gw_storage_provider
          envkey_PGUSER: postgres
          envkey_PGPASSWORD: postgres
          directory: src/examples/s3-storage

      - name: Build and Setup App and Postgres Database
        working-directory: src/packages/end-to-end
        timeout-minutes: 4
        run: |
          pnpm import-storage-provider
        env:
          CI: true
          PGDATABASE: gw_storage_provider
          PGUSER: postgres
          PGPASSWORD: postgres

      - name: Start and Test Storage Provider Example
        working-directory: src/packages/end-to-end
        timeout-minutes: 4
        run: |
          pnpm start-server &
          sleep 5 &&
          pnpm test-storage-provider &&
          pnpm test-ui-storage-provider &&
          killall node

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: playwright-report
          path: src/packages/end-to-end/playwright-report/
          retention-days: 30
        env:
          CI: true
