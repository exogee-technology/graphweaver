{
	"name": "federation-integration-test",
	"type": "module",
	"scripts": {
		"test": "npx fedtest docker --port 9001 --compose docker-compose.yaml --schema schema.graphql"
	},
	"dependencies": {
		"@apollo/federation-subgraph-compatibility": "^2.2.0"
	}
}
