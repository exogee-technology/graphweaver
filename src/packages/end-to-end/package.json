{
	"name": "@exogee/graphweaver-end-to-end",
	"version": "0.2.0",
	"description": "Graphweaver Test Suite",
	"license": "MIT",
	"scripts": {
		"create-app": "rm -rf ./app && node ../cli/bin init --name=app --backend=sqlite --version=\"local\" && cd app && pnpm i --ignore-workspace --no-lockfile",
		"import-database": "cp ./database.sqlite ./app/database.sqlite && cd app && node ../../cli/bin import sqlite --database=database.sqlite",
		"start-server": "cd app && pnpm start",
		"dev": "pnpm create-app && pnpm import-database && pnpm start-server",
		"test": "jest --runInBand",
		"version": "npm version --no-git-tag-version"
	},
	"devDependencies": {
		"@types/jest": "26.0.23",
		"graphql": "16.6.0",
		"graphql-tag": "2.12.6",
		"jest": "29.4.0",
		"supertest-graphql": "1.1.4",
		"ts-jest": "29.0.5",
		"typescript": "5.0.2"
	}
}
