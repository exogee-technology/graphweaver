{
	"name": "@exogee/graphweaver-end-to-end",
	"version": "0.0.1",
	"description": "Graphweaver Test Suite",
	"license": "MIT",
	"scripts": {
		"backup-pnpm-workspace": "mv ../../pnpm-workspace.yaml ../../pnpm-workspace.yaml.bak && cp ./pnpm-workspace.bak ../../pnpm-workspace.yaml",
		"create-app": "rm -rf ./app && node ../cli/bin init --name=app --backend=sqlite --version=\"workspace:*\" && cd app && pnpm i --no-lockfile",
		"restore-pnpm-workspace": "rm ../../pnpm-workspace.yaml && mv ../../pnpm-workspace.yaml.bak ../../pnpm-workspace.yaml",
		"import-database": "cp ./database.sqlite ./app/database.sqlite && cd app && node ../../cli/bin import sqlite --database=database.sqlite",
		"start-server": "cd app && pnpm start",
		"dev": "pnpm backup-pnpm-workspace && pnpm create-app && pnpm restore-pnpm-workspace && pnpm import-database && pnpm start-server",
		"test": "jest --runInBand"
	},
	"devDependencies": {
		"@types/jest": "26.0.23",
		"graphql": "16.6.0",
		"graphql-tag": "2.12.6",
		"jest": "29.4.0",
		"supertest-graphql": "1.1.4",
		"ts-jest": "29.0.5",
		"typescript": "5.0.2"
	}
}
